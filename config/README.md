This folder contains environment-specific configuration files.

Create a copy of `config.example.json` and rename it to `config.json`,
then add your local MySQL credentials.

This file is intentionally excluded from version control to avoid
committing sensitive information.